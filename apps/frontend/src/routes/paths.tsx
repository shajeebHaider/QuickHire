export const rootPaths = {
  root: '/'
};

export const paths = {
  home: rootPaths.root,
  jobs: '/jobs',
  contact: '/contact',
  dashboard: '/dashboard',
  profile: '/profile',
  settings: '/settings'
};

export const endpoints = {};
